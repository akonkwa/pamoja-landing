# PAMOJA Landing Page

Static marketing site for PAMOJA, deployed with a minimal Node server on Railway.

## Local run

```bash
npm start
```

The app serves files from `public/` and uses `PORT` from the environment when available.
